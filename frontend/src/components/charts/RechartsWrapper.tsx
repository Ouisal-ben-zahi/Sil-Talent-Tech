'use client'

// Ré-export direct depuis recharts avec dynamic import
// Next.js gère automatiquement le code splitting pour les imports dynamiques
export {
  BarChart,
  Bar,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  Legend,
  ResponsiveContainer,
  Cell,
} from 'recharts'

